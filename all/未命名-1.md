```mermaid
[ Start ]
   │
[ Initialize NP samples using LHS ]
   │
   ▼
[(A) Database (DB) ] <──────────────────────────────────────┐
   │                                                        │
   ▼                                                        │
[ Train Global RBF (top-gs) ]                               │
   │                                                        │
   ▼                                                        │
< Evaluate Progress P > ──── (P<=0.3) ──> [ Standard SLPSO ]│
   │                           │                            │
   ├─ (0.3<P<=0.8) ─> [ SLPSO + ACG (T=0.1) ]               │
   │                           │                            │
   └─ (P>0.8) ───────> [ SLPSO + ACG (T=0.05) ]             │
                    
                           │                            │
                               ▼                            │
                    < Duplicate in DB? > ──(Y)──> [ Add Noise ]
                               │(N)                         │
                               ▼                            │
                   [ Exact fitness evaluation ]             │
                               │                            │
                               ▼                            │
                   [ Update DB with global best ] ──────────┤ (全局更新反馈)
                               │                            │
   ┌───────────────────────────┘                            │
   ▼                                                        │
[ Train Local RBF (top-ls) ]                                │
   │                                                        │
   ▼                                                        │
< Evaluate Progress P > ──── (P<=0.3) ──> [ Standard FPSO ] │
   │                           │                            │
   ├─ (0.3<P<=0.8) ─> [ FPSO + Adaptive Perturbation(T=0.2)]│
   │                           │                            │
   └─ (P>0.8) ───────> [ Deep FPSO (T=0.05, 100D iters) ]   │
                               │                            │
                               ▼                            │
                    < Duplicate in DB? > ──(Y)──> [ Add Noise ]
                               │(N)                         │
                               ▼                            │
                   [ Exact fitness evaluation ]             │
                               │                            │
                               ▼                            │
                       < Stop Condition? > ──(N)────────────┘
                               │(Y)
                               ▼
                           [ Output ]
  
```